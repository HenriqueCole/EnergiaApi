# Ambiente de staging (homologacao).
# As portas sao diferentes das de producao pra os dois rodarem lado a lado.
ASPNETCORE_ENVIRONMENT=Staging
API_PORT=8081
DB_PORT=14331
DB_NAME=EnergiaDb_Staging

# Valores de fallback. No pipeline, os secrets do environment "staging" tem prioridade.
DB_PASSWORD=Energia@2026!
JWT_KEY=chave-staging-com-no-minimo-32-caracteres-aqui!!
